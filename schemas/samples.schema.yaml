$schema: "http://json-schema.org/draft-07/schema#"

description: an entry in the sample sheet
properties:
    sample:
        type: string
        description: sample name/identifier
    diffexp:
        type: boolean
        description: > 
            whether or not to include the sample in the differential 
            expression analysis

required:
    - sample
    - diffexp
